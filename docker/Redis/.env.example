# Redis 本地编排环境变量样例
# 复制为 .env 后填值;.env 已被 .gitignore 忽略
# 业务侧 application-dev.yaml 通过 REDIS_HOST / REDIS_PORT / REDIS_PASSWORD 读取,
# 端口必须与 compose 中映射一致(默认 6379)。

# 鉴权密码(留空则禁用密码,仅本地无外网环境推荐)
# 生成方式: openssl rand -base64 24
REDIS_PASSWORD=your-strong-redis-password
