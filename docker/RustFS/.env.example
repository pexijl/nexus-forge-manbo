# RustFS 本地编排环境变量样例
# 复制为 .env 后填值;.env 已被 .gitignore 忽略
# 注意:RustFS 与 MinIO 端口一致(9000/9001),如果同时启用需要修改端口映射

# 鉴权(必填,生产环境务必替换为强随机)
# 生成方式: openssl rand -base64 32
RUSTFS_ACCESS_KEY=rustfsadmin
RUSTFS_SECRET_KEY=rustfsadmin
